const letters = ['A', 'B', 'C','D'];

// ForEach

letters.forEach((el,idx,array) => {
    // console.log(el);
    console.log(`Номер елементу: ${idx}, сам елемент: ${el}`);
    
    
    
})

// console.log(letters.lastIndexOf('A',3));

// console.log(letters.indexOf());



// console.log(
//     users.findIndex((user) => user.name === 'Ihor')
// );


// DOM


